//import { inject } from '@angular/core';
import { Routes } from '@angular/router';

export const routes:Routes = []
/*export const ordersResolver: ResolveFn<any> = () => {
  return inject(OrderService).getOrders().subscribe({
    next: (order) => {
      console.log('✅ Dades inicials de comandes carregades');
      console.log(order);
      return order;
    },
    error: (err) => {
      console.log('❌ Error carregant les dades inicials:', err);
    }
  });
};
*/

